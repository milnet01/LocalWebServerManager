# P03b — journal

## 2026-08-15 — close attempted and BLOCKED; FP07 generated

Five items shipped into this phase on 2026-08-14 — LWSM-1009 (supervisor),
LWSM-1007 (registry writer), LWSM-1131 (rescan merge), LWSM-1010 (start / stop /
restart), LWSM-1016 (open in browser) — with no audit and no review between
them. This close ran `check-code` and `/code-quality-review` once, per the
standing 2026-08-07 decision.

**Three lanes, 55 findings, no false positives.** 3 CRITICAL, 7 HIGH, 19 MEDIUM
and a LOW/INFO tail. Scope was put to the user before any fix work: CRITICAL +
HIGH into `FP07` (LWSM-1132 … LWSM-1141), the rest to `docs/known-issues.md` as
known-issue-037 … 056 with named owners. Ten items folded; the phase stays 🚧.

**The headline is a correction, not a finding.** `.claude/workflow.md` recorded
success criterion 2 as closed end to end on 2026-08-14. It is closed for
**shell-launcher projects only**. `_launcher_path` (`supervisor.py:261`) builds
`(project / argv[0]).resolve()` for every argv, so a PATH-resolved command
yields `<project>/npm` — a file that does not exist — and `validate_launcher`
refuses it. The function contradicts its own docstring, which says
`["npm", "run", "dev"]` has no launcher file and that returning `None` there "is
honest rather than convenient". The scanner emits `npm`, `python3` and `node`
argvs; only `./start.sh` survives. Reproduced against the installed module
before it was written down.

**The cross-cutting finding is the third instance of a pattern this project
keeps meeting: a documented mechanism with no caller.** All three lanes found
one, in three different modules, with no sight of each other's reports —
`rotate_if_needed` (the 5 MB per-project log cap, zero production callers),
`DETECTED_FIELDS` / `USER_FIELDS` (the merge's classification spine, zero
non-declaration readers), and `wait_for_abandoned_probes`. FP06 recorded the
non-`OSError` whole-scan crash as a class only after fixing four separate
instances of it. This is the same shape caught at three, and it is named in
FP07's preamble as a class rather than patched three times.

**The second theme is LWSM-1069's shape, three more times.** An exception
escaping a Qt slot leaves a control permanently disabled with no message. The
raw `OSError` from `mkstemp` (`registry.py:819`, the only syscall in the writer
outside a handler), the `AttributeError` from an unset `load`, and the unbounded
optimistic overlay all end there. LWSM-1069 fixed this on the poll loop and
nowhere else — and `_finish_rescan` sits after the try block rather than in a
`finally`, which is what turns any escape into a dead Rescan button.

**Why 494 green tests said nothing about any of it.** Every `start()` test in
`test_supervisor.py` uses `("./start.sh",)` — a launcher-kind monoculture, so
the one branch that works is the only branch exercised. No fixture has a
port-less project, which is what hides the unbounded overlay. No fixture fills a
disk. This is the one-row-fixture trap recorded on 2026-08-14, one layer up: a
fixture set that cannot express the variation the code branches on. Two of
FP07's ten need a fixture class that does not exist yet, and that is the real
cost of the pass.

**Static analysis was clean for the fifth close running, and two of the zeros
carry less than they look.** semgrep ships a default ignore that excludes test
directories, so its zero is a statement about `src/` only — and it has been read
as whole-tree on all five closes. `pyright` reports 24 errors and is not in the
gate at all (LWSM-1066, open since 2026-08-06); 9 are in P03b code, including
the unguarded `self.layout()` returning `None` that known-issue-015 already
describes, now recurring in new code. `shfmt` did not run: the `.editorconfig`
has only a blanket `[*]` section, which is not a declared shell style.

**`typos` produced 20 findings and every one is false**, which is worth
recording so the next close does not re-read them: `mis` ×4 is `mis-detection`
(the known hyphenated-prefix calibration), `unparseable` ×6 and `unparented` ×2
are a valid variant and a Qt term, `THM` ×6 is a mermaid node id in `design.md`,
and `versoin` ×2 is a **deliberate** misspelling in `test_main.py` — the fixture
exists to prove a typo'd flag is refused.

**One decision taken with the user that outlives this pass.** ADR-0004 requires
a disclosure dialog before Open-in-browser on a foreign server; LWSM-1016's
bullet says enable it everywhere; the code followed the bullet. **The ADR
governs** — it carries the threat model. The full mitigation needs P06's
seven-state model, since `_classify` produces three states today and cannot tell
foreign from managed, so LWSM-1141's scope is the interim: restrict Open to
servers this manager started, and correct LWSM-1016's bullet.

**Deliberate deviation:** no CHANGELOG entry was written for FP07, following the
FP06 precedent — nothing is fixed yet and `[Unreleased]` is public.
