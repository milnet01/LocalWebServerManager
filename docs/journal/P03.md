# P03 — Project discovery

## 2026-08-12 — Close attempted; 25 findings, 9 folded into FP06, phase stays 🚧

**Close of LWSM-1006 BLOCKED.** `/audit` + `/code-quality-review` ran once, per
the standing 2026-08-07 process decision. Nine actionable findings became
`FP06` (LWSM-1122…1130); sixteen routed to `docs/known-issues.md`
(known-issue-017…033) with named owners. **No false positives** — nothing was
added to `docs/audit-allowlist.md` this pass, and the existing eight entries
pre-discarded nothing that came up.

### What the tools found: nothing, for the fourth close running

ruff (check + format, 59 files), bandit (2116 lines), semgrep, gitleaks,
shellcheck — all zero. **The zero had to be verified rather than accepted.**
`audit_run` reported `total_raw: 0` with `artifacts: 0` on every tool, which is
indistinguishable from a sweep that read nothing, so ruff and bandit were re-run
by hand inside the venv. Bandit counting 2116 lines is the evidence the sweep
was real. Filed upstream as an Ants MCP finding: a verb reporting zero should
say what it looked at.

### The cross-cutting finding: a fourth non-`OSError` whole-scan crash

Two lanes with different reproducers landed on the same defect, which is the
strongest signal this format produces. `Path.exists` / `is_symlink` / `is_file`
re-raise `EACCES` and `ENAMETOOLONG` on Python 3.13 — `pathlib` only swallows
`ENOENT/ENOTDIR/EBADF/ELOOP` — and four call sites sit outside any handler while
`scan()` catches only `_BudgetExpired`.

Both triggers were reproduced a second time, independently, before being
written down:

- `chmod 000` on any candidate directory → **20 healthy projects returned 0**.
  Needs no attacker.
- A 3000-character hop token in a `start.sh` → `OSError [Errno 36]`. One line,
  attacker-authored.

**This is the fourth distinct shape of the same class** after the `TypeError`,
`KeyError` and `ValueError` the spec gate found. Three loops fixed three
instances; none of them fixed the class. LWSM-1122 fixes it structurally, with
per-candidate containment, which is the only reason to expect a fifth not to
arrive.

### The half that is harder to sit with: 370 green tests proving less than they look

81 mutants were applied to `scanner.py`. **47 went red; 34 did not.** Three
clauses the spec calls load-bearing are correct in the code and protected by
nothing:

- `_BudgetExpired` can be made an `OSError` and all 182 scanner tests stay
  green — while a timed-out scan then reports `timed_out=False`, i.e. claims
  to be complete. LWSM-1007 is about to persist that list.
- The `package.json` dependency-block scope — the spec review's single
  highest-value finding — can be undone silently.
- Rule 1's execute-bit precondition has zero coverage; `scanner.py:943` never
  executes.

**The suite is strong** — 47 reds, all 12 corpus fixtures assert exact
expectations with no "something was detected" anywhere, and every invariant a
reader would guess was "really" tested is tested. The gap is specific, not
general.

### Two things worth carrying forward

**A green-by-construction fixture survived its own correction.** § 12 already
records INV-15's fixture being fixed once for exactly this — `"a"*4092+"port"`
had no separator, so the rule returned early. The replacement reaches
`KEY_IS_PORT` and still makes **zero** calls to the digit scan the invariant
names. Instrumenting the call count, not reading the fixture, is what saw it
(known-issue-026).

**Three sanitiser sweeps in this project have now stopped one call site short.**
`scanner.py:605` is the last unescaped reason (measured at 248 and 635
characters against a 120 cap, carrying live ANSI escapes) and `:467`'s
`PortFinding.source` is sanitised but untested. Both are INV-18 clauses. The
pattern is strong enough that a future sweep should assert the invariant
across all call sites rather than fix the ones it can find.

### Process notes

- **Spec re-gate: waived, by the user, on the record.** § 4.6's rule-3 table was
  authored-edited during implementation (Vite: substring → whole-word), which
  global rule 14 says re-arms the gate. Waived on two grounds — the edit was
  forced by an executed acceptance test rather than judgement, and the user's
  standing instruction for this item is no further review loops. Recorded in
  `.claude/workflow.md § 1` as an exemption, not a precedent.
- **No CHANGELOG entry was written for FP06**, deliberately, against
  `/close-phase` step 5a-3. Nothing is fixed yet, and `[Unreleased]` is a public
  file; a `### Fixed` line for unfixed work is the exact class `/review-ledger`
  exists to catch. FP06's entries land with its fixes.
- **The ratio was put to the user before any work started**, per the convergence
  checkpoint's standing promise. P03 is one feature item that has produced 25
  findings after seven spec-review loops. The user chose the nine above the bar
  and routed the rest.

---

## 2026-08-12 — FP06 closed, and P03 with it

All nine bullets ✅ in six commits, one per bullet-group, each with its red test
watched failing first. **386 tests, up from 370.** Full gate green; `/audit`
clean for the fifth close running, with the coverage numbers recorded this time
rather than a bare zero: ruff over 60 files, bandit over **2129 lines**, semgrep
**10 targets / 200 rules / 0 findings**, gitleaks **145 commits / 2.41 MB**,
shellcheck 1 script.

`/code-quality-review` was **not** re-run, by the user's decision and per the
2026-08-07 standing rule of one audit + one review per phase. FP06's own ~350
new lines therefore ship unread by a cold reviewer; that is the trade the
standing rule makes and it is recorded here rather than left implicit.

### Three bullets were wrong or incomplete about their own mechanism

The section above closes with "a fold-in bullet is a reviewer's reading" as a
pattern from FP05. It held again, at three of nine:

- **LWSM-1126's prescribed mutation is inert.** `dependencies` is a set of
  **keys**, so appending `*sorted(dependencies)` scans `get-port` — no digits,
  no port, suite stays green. The scope breach that is real is reading
  `package.json` as an ordinary source file, and the fixture had to be
  **pretty-printed** to detect it: minified, rule 2 stops at the document's
  first `:` and never reaches the dependency pair. A fixture that cannot fail is
  precisely what the bullet existed to remove.
- **LWSM-1124's test cannot see its own bound being loosened.** Measured after
  writing it: `MAX_REASON_CHARS = 400` leaves it green. That is
  known-issue-005's shape in the module written *after* that issue closed, and
  it is now known-issue-034.
- **LWSM-1122 and LWSM-1125 interlock, and neither bullet says so.** 1122 adds a
  per-candidate `except OSError` to `scan()`; under 1125's mutation
  (`_BudgetExpired(OSError)`) it is *that new handler* which swallows the budget
  signal. 1125's behavioural test is now what stands between the containment and
  a truncated scan reporting itself complete.

### Two findings routed rather than fixed

known-issue-034 (scanner's bounds pinned by nothing → LWSM-1007) and
known-issue-035 (`test_completed_tasks_do_not_accumulate` failed once in ~20
runs and did not reproduce → LWSM-1011). The second is recorded rather than
dismissed: this project has shipped three times against a green run that was not
one, and an unexplained red is the same report in the other direction.
